pragma solidity 0.5.0;
import  "./Organization.sol";
//var address; OrganizationCreator.deployed().then(function(depltruffle(develop)> var address; OrganizationCreator.deployed().then(function(deployed){address=deployed;});
//address.Create("software factory","yarab").then(function(data){console.log(data.logs[0].args[2]);});


contract OrganizationCreator {
     /*  string organizationName;
      string organizationDiscription;
      Organization organization;
     /*  constructor (address _organization) public{
          organization = Organization(_organization);
      } */ 
   /*    mapping (address => uint) organizations;
      event organizationCreated(string name,string discription ,address organizationAddress);
      function Create(string memory _organizationName,string memory _organizationDiscription) public {
       organization = new Organization(_organizationName,_organizationDiscription);
        organizations[address(organization)] = 7;
        emit organizationCreated(_organizationName,_organizationDiscription,address(organization));  
        
      }
      function GoTo(address _organizationAddress) view public returns (bool) {
        uint size;
        assembly {
          size := extcodesize(_organizationAddress)
          }
          return size > 0;
     } */
}